coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto

ignore:
  - "custom_components/rainpoint_spenceh14/debug.py"
  - "tests/"
